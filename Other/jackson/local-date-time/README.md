# LocalDateTime

In order to serialize and deserialize `LocalDateTime` objects, you need to add the `implementation("com.fasterxml.jackson.datatype:jackson-datatype-jsr310:2.20.1")` dependency and configure the ObjectMapper, see inside dude.